%%writefile index.html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Belinda's Birthday Surprise!</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <!-- Password Entry Screen -->
    <section id="password-section">
        <h1>Enter the secret code!</h1>
        <input type="password" id="password-input" placeholder="Your secret code">
        <button id="submit-password">Enter</button>
        <p id="password-message"></p>
    </section>

    <!-- Dynamic Countdown Display (initially hidden) -->
    <section id="countdown-section" class="hidden">
        <h1>Time until Belinda's Birthday!</h1>
        <div class="countdown-timer">
            <div class="time-block">
                <span id="days">00</span>
                <span class="label">Days</span>
            </div>
            <div class="time-block">
                <span id="hours">00</span>
                <span class="label">Hours</span>
            </div>
            <div class="time-block">
                <span id="minutes">00</span>
                <span class="label">Minutes</span>
            </div>
            <div class="time-block">
                <span id="seconds">00</span>
                <span class="label">Seconds</span>
            </div>
        </div>
    </section>

    <!-- 'Happy Birthday B-E-L-I-N-D-A' Animated Text (initially hidden) -->
    <section id="birthday-text-section" class="hidden">
        <h1>Happy Birthday B-E-L-I-N-D-A!</h1>
    </section>

    <!-- Container for Fireworks Animation (initially hidden) -->
    <section id="fireworks-section" class="hidden">
        <canvas id="fireworks-canvas"></canvas>
        <!-- New shutdown countdown element -->
        <div id="shutdown-countdown" class="hidden"></div>
    </section>

    <!-- Audio Element -->
    <audio id="birthday-audio" src="audio/happy_birthday.mp3" preload="auto" loop="false"></audio>

    <script defer src="script.js"></script>
</body>
</html>